package Player.Regions;

public interface AuthorEnvironment {
    /**
     * Will launch the authoring environment user interface, allowing the user to create new games and levels. To be
     * called from the launcer (main).
     */
    void launch();
}
